# Upstash Redis Configuration (Optional: Used for API Rate Limiting on /api/contact)
# If not provided, the API and middleware fail open gracefully.
UPSTASH_REDIS_REST_URL=https://your-redis-instance-name.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token
